#include <stdio.h>

int main(){
    int arr[5];
    int size,n;
    printf("Enter the number:");
    scanf("%d",&n);
    size=sizeof(arr)/sizeof(arr[0]);
    for(int i=0; i<= size;i++){
        arr[i]==n;
    }
        for(int j=0; j<= size;j++){
        printf("The array values",arr[j]);
    }
    
}
